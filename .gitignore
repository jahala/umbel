# no trailing slash: also ignores a stray `node_modules` *symlink* (a dir pattern
# does not, which let a build symlink get committed in #33 and broke CI on checkout)
node_modules
dist/
.umbel-state/
*.log
.DS_Store
.context/
.env
.env.local
*.tsbuildinfo
coverage/
docs/research/
.brand/
